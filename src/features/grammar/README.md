# Grammar feature (scaffold)

Not implemented yet. See `plan-phase-2.md` → Feature 3 (Grammar Practice).
Will contain: LevelCard, GrammarSelector, ExerciseCard, AnswerInput, FeedbackCard, HintCard
components, plus service calls into `core/ai` using the `grammar-generate` /
`grammar-check` prompts.
