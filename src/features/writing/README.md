# Writing feature (scaffold)

Not implemented yet. See `plan-phase-2.md` → Feature 2 (Writing Practice).
Will contain: WritingEditor, EvaluationCard, GrammarErrorCard, CorrectedText components,
plus a service call into `core/ai` using the `writing` prompt.
